type State = {
  errorName: string;
  name: string;
};

type DateError = {
  name: string;
  errorName: string;
};

// eslint-disable-next-line import/prefer-default-export
export type { State, DateError };
